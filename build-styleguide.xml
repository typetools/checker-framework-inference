<project name="checkerframework" basedir="." default="check-style">

    <property name="checkerframework" value="${basedir}/../checker-framework"/>

    <property file="${checkerframework}/build-common.properties"/>

    <fileset id="formatted.java.files" dir="." includes="**/*.java" excludes=""/>

    <import file="${checkerframework}/build-styleguide.xml"/>

</project>
